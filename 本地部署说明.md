# TestFlow-mall 本地部署说明

被测系统：`macrozheng/mall`（master 分支，Spring Boot 3.5 / JDK 17）

部署日期：2026-09-07

---

## 一、当前运行状态

| 组件 | 端口 | 状态 | 说明 |
|---|---|---|---|
| MySQL 8.0 | 3306 | ✅ 系统服务自启 | mall 数据库已导入 |
| Redis 5.0.14 | 6379 | ✅ 已启动 | 手动启动，重启电脑后需重新启动 |
| mall-admin 后端 | 8080 | ✅ 已启动 | Spring Boot fat jar |
| mall-admin-web 前端 | 5173 | ✅ 已启动 | Vite 开发服务器 |

## 二、访问入口

- **管理后台**：http://localhost:5173
- **后端 API**：http://localhost:8080
- **Swagger 文档**：http://localhost:8080/swagger-ui.html

### 登录账号

| 账号 | 密码 | 角色 |
|---|---|---|
| admin | macro123 | 超级管理员 |
| test | 123456 | 普通测试账号 |

## 三、数据库

- 库名：`mall`
- 专用账号：`mall` / `mall123456`（仅授权 mall 库，不影响 root）
- 初始化脚本：`C:\Users\zhoub\Desktop\TestFlow-mall\mall\document\sql\mall.sql`

## 四、各组件启动 / 停止

### 1. Redis（重启电脑后需手动启动）

```powershell
Start-Process -FilePath "C:\Users\zhoub\tools\redis\redis-server.exe" -ArgumentList "--port","6379" -WindowStyle Hidden
# 验证
& "C:\Users\zhoub\tools\redis\redis-cli.exe" ping   # 返回 PONG
```

### 2. mall-admin 后端

```powershell
# 启动
$env:JAVA_HOME = "C:\Program Files\Java\jdk-17"
& "C:\Program Files\Java\jdk-17\bin\java.exe" -jar "C:\Users\zhoub\Desktop\TestFlow-mall\mall\mall-admin\target\mall-admin-1.0-SNAPSHOT.jar"

# 停止：结束监听 8080 的 java 进程
```

### 3. mall-admin-web 前端

```powershell
cd "C:\Users\zhoub\Desktop\TestFlow-mall\mall-admin-web"
npm run dev
```

### 4. MySQL

Windows 服务 `MySQL80`，开机自启，一般无需操作。

## 五、环境清单

| 组件 | 位置 |
|---|---|
| JDK 17 | C:\Program Files\Java\jdk-17（JAVA_HOME 原指向 JDK 8，构建时需显式指定） |
| Maven 3.9.11 | C:\Users\zhoub\tools\apache-maven-3.9.11 |
| Redis | C:\Users\zhoub\tools\redis |
| mall 源码 | C:\Users\zhoub\Desktop\TestFlow-mall\mall |
| mall-admin-web 源码 | C:\Users\zhoub\Desktop\TestFlow-mall\mall-admin-web |

## 六、部署中解决的问题

### JDK 17 + BouncyCastle JCE 认证失败

**现象**：登录接口返回 500 / 401，后端日志报
`JCE cannot authenticate the provider BC`，
根因是 `bcprov-jdk18on-1.84.jar` 内含签名文件但
`META-INF/services/java.security.Provider` 条目未签名，
JDK 17 的 JCE 严格校验拒绝加载 BC provider，导致 hutool
JWT（HS256 签名）生成 token 失败。

**解决**：移除 fat jar 内 bcprov jar 的签名文件
（`META-INF/BCRSA204.SF` / `.RSA`），使其变为未签名 jar，
JCE 跳过验证，BC provider 正常加载。

> ⚠️ 注意：重新执行 `mvn clean install` 后 fat jar 会被重建，
> 需重新执行该修复（修复工具脚本见 `tools/` 说明，或重新联系助手处理）。

## 七、后续测试项目建议目录

按《最终锁定项目.md》规划，测试代码与被测系统分开管理：

```text
TestFlow-mall
├── docs/          # 测试计划、用例、缺陷报告
├── api-test/      # Python + Requests + Pytest 接口自动化
├── ui-test/       # Selenium + POM UI 自动化
├── performance-test/  # JMeter 脚本
├── database-test/ # PyMySQL 数据校验
├── test-data/
├── reports/
└── .github/       # GitHub Actions
```
