# 软件测试作品集项目实施方案

## 项目名称：TestFlow —— Mall 电商系统全流程自动化测试项目

------

## 一、项目最终目标

你最后要做出来的不是一个"练习项目"，而是一套可以直接放到简历和 GitHub 上的完整测试作品集。

最终成果包含：

- 1 个真实可运行的 Web 电商项目
- 1 份测试计划
- 100～150 条功能测试用例
- 20～40 个接口自动化测试
- 10～20 个 UI 自动化测试
- 数据库校验
- JMeter 性能测试
- Allure 自动化测试报告
- GitHub Actions 持续集成
- 缺陷记录
- 测试总结报告
- GitHub README
- 简历项目描述
- 面试 STAR 项目话术

项目周期建议：

**5 周左右。**

每天投入：

**2～4 小时。**

------

# 二、项目整体架构

整个作品集分成四层：

```text
被测系统
    ↓
功能测试
    ↓
自动化测试
    ↓
工程化测试
```

完整结构：

```text
Mall 电商系统
│
├── 功能测试
│   ├── 测试计划
│   ├── 测试用例
│   └── 缺陷报告
│
├── 接口自动化
│   ├── Python
│   ├── Requests
│   └── Pytest
│
├── UI自动化
│   ├── Selenium
│   ├── Pytest
│   └── POM
│
├── 数据库验证
│   ├── MySQL
│   └── PyMySQL
│
├── 性能测试
│   └── JMeter
│
└── 工程化
    ├── Git
    ├── GitHub
    ├── GitHub Actions
    └── Allure
```

------

# 三、第 0 阶段：准备环境

时间：

**1～2 天**

需要安装：

```text
JDK 17
MySQL 5.7
Redis 7.0
Python 3.11 / 3.12
PyCharm / IDEA
Git
Postman / Apifox
JMeter
Chrome
GitHub
```

Python 环境：

```bash
python -m venv venv
```

Windows：

```bash
venv\Scripts\activate
```

安装依赖：

```bash
pip install pytest
pip install requests
pip install selenium
pip install pymysql
pip install pyyaml
pip install allure-pytest
pip install faker
```

生成：

```text
requirements.txt
```

命令：

```bash
pip freeze > requirements.txt
```

------

# 四、第 1 周：测试基础 + 功能测试

这一周先不要急着写自动化。

你的目标是先像真正的软件测试工程师一样理解系统。

------

## Day 1：熟悉系统

把系统从头到尾操作一遍。

第一阶段先聚焦**后台管理系统（mall-admin）**，重点整理：

### 管理员模块

```text
登录
退出
个人信息
权限
```

### 商品模块

```text
商品列表
商品详情
商品搜索
商品分类
品牌管理
分页
```

### 订单模块

```text
订单列表
订单详情
订单发货
关闭订单
```

### 用户模块

```text
会员列表
会员详情
会员禁用/启用
```

### 权限模块

```text
用户管理
角色管理
菜单管理
资源管理
```

当天输出：

```text
docs/
└── 功能模块分析.md
```

内容例如：

```text
管理员模块
├── 登录
├── 退出
└── 权限

商品模块
├── 商品列表
├── 商品搜索
├── 商品分类
└── 品牌管理

订单模块
├── 订单列表
├── 订单详情
└── 订单发货

用户模块
├── 会员列表
└── 会员详情

权限模块
├── 用户管理
├── 角色管理
└── 菜单管理
```

等后台管理跑通后，第二阶段再扩展前台商城（mall-portal）：

```text
前台商城
├── 用户注册/登录
├── 商品搜索/详情
├── 购物车
├── 下单
└── 个人中心
```

------

# 五、Day 2：写测试计划

建立：

```text
docs/测试计划.md
```

内容包括：

## 测试目标

验证 Mall 电商系统后台管理核心功能是否正常。

## 测试范围

例如：

```text
管理员登录
商品管理
商品分类
品牌管理
订单管理
会员管理
权限管理
```

## 不测试内容

例如：

```text
第三方支付接口
短信验证码
外部推荐系统
Elasticsearch 搜索（第一阶段用 MySQL 搜索）
MongoDB 相关功能
```

## 测试类型

```text
功能测试
接口测试
UI自动化
数据库测试
性能测试
```

## 测试环境

```text
Windows 11
JDK 17
MySQL 5.7
Redis 7.0
Chrome
Python 3.11
```

## 测试风险

例如：

```text
测试数据不足
接口依赖数据库
自动化脚本受页面变化影响
Mall 项目模块多，环境搭建复杂
```

------

# 六、Day 3～5：编写功能测试用例

先做 100 条左右。

Excel 表格字段：

```text
用例编号
模块
功能
用例标题
优先级
前置条件
测试步骤
测试数据
预期结果
实际结果
是否通过
备注
```

------

## 登录模块案例

### CASE_LOGIN_001

正常登录。

```text
账号：admin
密码：123456
```

预期：

```text
登录成功
跳转后台首页
显示用户名
```

------

### CASE_LOGIN_002

密码错误。

预期：

```text
登录失败
提示账号或密码错误
```

------

### CASE_LOGIN_003

密码为空。

------

### CASE_LOGIN_004

用户名为空。

------

### CASE_LOGIN_005

连续点击登录按钮。

------

### CASE_LOGIN_006

密码长度边界：

```text
5位
6位
20位
21位
```

------

## 商品管理模块案例

### CASE_PRODUCT_001

正常新增商品。

```text
商品名称：测试商品001
商品分类：手机数码
品牌：华为
价格：2999
库存：100
```

预期：

```text
新增成功
商品列表中显示该商品
数据库 pms_product 表新增一条记录
```

------

### CASE_PRODUCT_002

商品名称为空。

预期：

```text
新增失败
提示商品名称不能为空
```

------

### CASE_PRODUCT_003

价格输入负数。

预期：

```text
新增失败
提示价格必须大于0
```

------

### CASE_PRODUCT_004

搜索不存在的商品。

预期：

```text
列表为空
提示"暂无数据"
```

------

# 七、测试用例设计方法

每个模块尽量体现这些方法：

```text
等价类
边界值
场景法
错误推测法
判定表
```

这样面试官问：

> 你是怎么设计测试用例的？

你就有东西可以讲。

------

# 八、Day 6～7：执行测试 + 提 Bug

不要刻意追求很多 Bug。

找到 5～10 个真实问题即可。

建立：

```text
docs/bug/
```

例如：

```text
BUG001.md
BUG002.md
BUG003.md
```

Bug 模板：

```text
Bug ID：

标题：

严重程度：

优先级：

测试环境：

前置条件：

复现步骤：

实际结果：

预期结果：

截图：

日志：
```

例如：

```text
BUG-001

标题：
搜索不存在商品时分页显示异常

严重程度：
Major

步骤：
1. 管理员登录后台
2. 进入商品管理
3. 搜索关键词 qwerty123456
4. 点击搜索

实际：
列表为空但分页仍显示总条数，页码可点击但无数据

预期：
列表为空，分页隐藏或显示"暂无数据"
```

------

# 九、第 2 周：接口测试

这一周是项目第一个重点。

学习顺序：

```text
HTTP
↓
REST API
↓
Postman / Apifox
↓
Requests
↓
Pytest
↓
接口自动化框架
```

------

# 十、Day 8：学习 HTTP

必须掌握：

```text
GET
POST
PUT
DELETE

Request
Response
Header
Body
Cookie
Token
JSON
HTTP状态码
```

重点状态码：

```text
200
201
400
401
403
404
500
```

------

# 十一、Day 9：手工接口测试

先不要自动化。

使用：

**Postman 或 Apifox**

测试后台管理接口：

```text
/admin/login

/admin/product/list

/admin/product/create

/admin/product/update/{id}

/admin/product/delete/{id}

/admin/productCategory/list

/admin/order/list

/admin/order/{id}

/admin/member/list
```

每个接口测试：

```text
正常参数
异常参数
空参数
边界值
权限问题（不带Token）
重复请求
```

Mall 后台接口统一返回格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

断言时重点验证 `code` 字段。

------

# 十二、Day 10：Requests

先写最简单代码：

```python
import requests

url = "http://localhost:8080/admin/login"

data = {
    "username": "admin",
    "password": "123456"
}

response = requests.post(url, json=data)

print(response.status_code)
print(response.json())
```

然后加断言：

```python
assert response.status_code == 200
assert response.json()["code"] == 200
```

------

# 十三、Day 11～12：Pytest

学习：

```text
pytest基本使用

assert

fixture

参数化

setup / teardown

conftest.py
```

测试：

```python
def test_login_success():
    ...

def test_login_wrong_password():
    ...

def test_login_empty_username():
    ...
```

------

# 十四、Day 13～14：搭建接口自动化框架

GitHub 目录：

```text
TestFlow-mall/
│
├── api/
│
│   ├── login_api.py
│   ├── product_api.py
│   └── order_api.py
│
├── common/
│
│   ├── request.py
│   ├── logger.py
│   ├── database.py
│   └── utils.py
│
├── config/
│
│   └── config.yaml
│
├── data/
│
│   └── test_data.yaml
│
├── testcases/
│
│   ├── test_login.py
│   ├── test_product.py
│   └── test_order.py
│
├── reports/
│
├── conftest.py
│
├── pytest.ini
│
└── requirements.txt
```

------

# 十五、封装 Request

common/request.py：

```python
import requests


class RequestUtil:

    def send_request(
        self,
        method,
        url,
        **kwargs
    ):

        response = requests.request(
            method=method,
            url=url,
            **kwargs
        )

        return response
```

后续统一调用。

------

# 十六、配置文件

config.yaml：

```yaml
test:
  base_url: http://localhost:8080

database:
  host: localhost
  port: 3306
  user: root
  password: root
  database: mall
```

避免把 URL 写死在测试代码里。

------

# 十七、参数化

例如登录：

```python
import pytest


@pytest.mark.parametrize(
    "username,password",
    [
        ("admin", "123456"),
        ("admin", "wrong"),
        ("", "123456"),
        ("admin", "")
    ]
)
def test_login(username, password):
    pass
```

这部分面试非常容易问。

------

# 十八、接口关联

登录：

```text
POST /admin/login
```

得到：

```text
token
```

后面的：

```text
商品管理
订单管理
会员管理
```

使用这个 Token。

例如：

```python
headers = {
    "Authorization": token
}
```

这是项目里非常重要的一个点。

Mall 后台的 Token 通常放在请求头中：

```python
headers = {
    "Authorization": f"Bearer {token}"
}
```

具体以实际接口返回和要求为准。

------

# 十九、第 3 周：数据库测试 + UI 自动化

------

## Day 15：数据库校验

安装：

```bash
pip install pymysql
```

代码：

```python
import pymysql


conn = pymysql.connect(
    host="localhost",
    user="root",
    password="root",
    database="mall"
)

cursor = conn.cursor()

cursor.execute(
    "select * from ums_admin where username='admin'"
)

result = cursor.fetchone()

print(result)
```

------

# 二十、接口 + 数据库验证

例如：

### 新增商品

接口：

```text
POST /admin/product/create
```

返回：

```text
操作成功
```

然后数据库：

```sql
SELECT *
FROM pms_product
WHERE name='测试商品001';
```

验证：

```text
商品确实存在
库存、价格等字段正确
```

------

## 订单发货

接口：

```text
POST /admin/order/update/delivery
```

然后查询：

```sql
SELECT status
FROM oms_order
WHERE id=1;
```

验证订单状态从"待发货"变为"已发货"。

------

# 二十一、Day 16：Selenium

学习：

```text
启动浏览器
打开网页
元素定位
点击
输入
等待
断言
```

基础：

```python
from selenium import webdriver
from selenium.webdriver.common.by import By


driver = webdriver.Chrome()

driver.get(
    "http://localhost:8090"
)

driver.find_element(
    By.ID,
    "username"
).send_keys("admin")
```

注意：mall-admin-web 默认运行在 8090 端口，以实际启动端口为准。

------

# 二十二、Day 17：等待机制

重点学习：

```text
隐式等待
显式等待
```

项目尽量使用：

**显式等待。**

不要疯狂：

```python
time.sleep(5)
```

------

# 二十三、Day 18：Page Object Model

目录：

```text
pages/
│
├── login_page.py
├── home_page.py
├── product_page.py
└── order_page.py
```

例如：

```python
class LoginPage:

    def __init__(self, driver):
        self.driver = driver

    def input_username(self, username):
        pass

    def input_password(self, password):
        pass

    def click_login(self):
        pass
```

------

# 二十四、Day 19～21：写 UI 自动化

重点流程：

### 场景 1

```text
管理员登录
```

### 场景 2

```text
登录
↓
商品管理
↓
搜索商品
↓
查看商品详情
```

### 场景 3

```text
登录
↓
商品管理
↓
新增商品
↓
验证商品列表显示
```

### 场景 4

```text
登录
↓
订单管理
↓
查看订单详情
↓
订单发货
```

### 场景 5

```text
登录
↓
权限管理
↓
新增角色
↓
分配菜单权限
```

不要做几十个 UI 脚本。

**10～20 条核心业务即可。**

UI 自动化的重点是：

> 证明你懂框架设计。

------

# 二十五、第 4 周：性能测试 + 测试报告

------

# 二十六、Day 22：学习 JMeter

掌握：

```text
Thread Group

HTTP Request

CSV Data Set Config

HTTP Header Manager

JSON Extractor

Assertion

Listener
```

------

# 二十七、Day 23：登录性能测试

设计：

```text
10用户
50用户
100用户
300用户
```

Ramp Up：

```text
10秒
```

持续：

```text
1～5分钟
```

记录：

```text
平均响应时间
P90
P95
TPS
错误率
```

注意：Mall 后台登录接口可能有验证码或限流，压测前先确认是否需要绕过验证码，或选择无验证码的接口进行压测。

------

# 二十八、Day 24：商品列表接口测试

测试：

```text
10并发

50并发

100并发

300并发
```

接口：

```text
GET /admin/product/list
```

记录：

```text
并发用户
TPS
平均响应时间
P95
错误率
```

最终形成：

```text
docs/性能测试报告.md
```

------

# 二十九、Day 25：混合场景

模拟后台管理日常操作比例：

```text
40% 商品列表查询

30% 商品搜索

15% 登录

10% 订单列表查询

5% 新增商品
```

这个比单接口压测更有项目感。

------

# 三十、Day 26：Allure

安装：

```bash
pip install allure-pytest
```

运行：

```bash
pytest --alluredir=reports/allure-results
```

生成报告：

```bash
allure serve reports/allure-results
```

最终报告包含：

```text
测试数量

成功数量

失败数量

成功率

执行时间

失败日志
```

------

# 三十一、第 5 周：工程化 + GitHub + 简历

这一周是整个项目最影响"简历观感"的阶段。

------

# 三十二、Day 27：日志模块

common/logger.py：

```python
import logging


logging.basicConfig(
    level=logging.INFO,
    format=(
        "%(asctime)s "
        "%(levelname)s "
        "%(message)s"
    )
)

logger = logging.getLogger(__name__)
```

执行时：

```text
2026-09-05 INFO login test start

2026-09-05 INFO request /admin/login

2026-09-05 INFO response code 200

2026-09-05 INFO test passed
```

------

# 三十三、Day 28：失败截图

UI 自动化失败：

```text
自动截图
```

存：

```text
reports/screenshots/
```

例如：

```text
test_login_failed_20260905.png
```

然后附加到 Allure。

这会明显提高项目完整度。

------

# 三十四、Day 29：GitHub Actions

创建：

```text
.github/
└── workflows/
    └── test.yml
```

基本流程：

```yaml
name: Auto Test

on:
  push:
  pull_request:

jobs:

  test:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install -r requirements.txt

      - run: pytest
```

达到：

```text
提交代码
↓
自动运行
↓
pytest
↓
测试通过 / 失败
```

注意：接口自动化测试依赖 Mall 后端服务，CI 中需要考虑是否启动 Mall 服务，或者只运行不依赖服务的单元测试/Mock 测试。可以在 CI 中只运行接口层的 Mock 测试，或者使用 Docker Compose 启动依赖环境。

------

# 三十五、最终 GitHub 目录

最终建议：

```text
TestFlow-mall
│
├── README.md
│
├── requirements.txt
│
├── pytest.ini
│
├── conftest.py
│
├── config
│   └── config.yaml
│
├── api
│   ├── login_api.py
│   ├── product_api.py
│   └── order_api.py
│
├── common
│   ├── request.py
│   ├── database.py
│   ├── logger.py
│   └── utils.py
│
├── pages
│   ├── login_page.py
│   ├── home_page.py
│   ├── product_page.py
│   └── order_page.py
│
├── testcases
│   ├── api
│   └── ui
│
├── data
│   └── test_data.yaml
│
├── performance
│   ├── login.jmx
│   └── product_list.jmx
│
├── docs
│   ├── 测试计划.md
│   ├── 测试用例.xlsx
│   ├── 缺陷报告.xlsx
│   ├── 性能测试报告.md
│   └── 测试总结.md
│
├── reports
│
└── .github
    └── workflows
        └── test.yml
```

------

# 三十六、README 必须写什么

GitHub 首页至少包括：

```text
项目介绍

测试对象（Mall 电商系统）

项目架构

技术栈

测试范围

自动化框架设计

接口测试

UI自动化

数据库验证

性能测试

CI/CD

Allure测试报告

项目运行方式

项目成果
```

最好放 4～6 张图片：

```text
Mall 后台首页

Postman接口测试

Pytest运行结果

Allure报告

JMeter报告

GitHub Actions
```

------

# 三十七、项目完成标准

不要追求代码数量。

达到下面这些就可以收尾：

```text
功能测试用例：
100～150条

Bug：
5～15个

接口自动化：
20～40条

UI自动化：
10～20条

数据库验证：
5～10个业务场景

性能测试：
3个场景

Allure：
完成

GitHub Actions：
完成

README：
完成
```

------

# 三十八、每天具体执行表

## 第 1 周

```text
D1 系统分析（Mall 后台功能模块梳理）

D2 测试计划

D3 登录/权限用例

D4 商品管理用例

D5 订单/会员用例

D6 执行测试

D7 Bug整理
```

## 第 2 周

```text
D8 HTTP

D9 Postman/Apifox（Mall 后台接口）

D10 Requests

D11 Pytest

D12 Fixture + 参数化

D13 自动化框架

D14 接口测试（商品、订单）
```

## 第 3 周

```text
D15 MySQL数据库验证

D16 Selenium

D17 元素定位 + 等待

D18 POM

D19 登录自动化

D20 商品管理自动化

D21 订单管理自动化
```

## 第 4 周

```text
D22 JMeter

D23 登录压测

D24 商品列表压测

D25 混合场景

D26 Allure
```

## 第 5 周

```text
D27 Logger

D28 失败截图

D29 GitHub Actions

D30 README

D31 测试总结

D32 简历

D33 面试复盘
```

------

# 三十九、你在学习过程中需要重点掌握的知识

不要什么都学。

校招测试岗优先级：

## 第一优先级

```text
软件测试基础
Linux基础
MySQL
HTTP
接口测试
Python
pytest
```

## 第二优先级

```text
Selenium
JMeter
Git
```

## 第三优先级

```text
Allure
Jenkins
GitHub Actions
Docker
```

------

# 四十、项目面试介绍

面试官：

> 介绍一下你的测试项目。

你可以这样讲：

我做了一个针对 Mall 电商系统后台管理的完整软件测试项目。

项目中我首先根据业务需求进行了测试点分析，梳理了商品管理、订单管理、会员管理、权限管理等核心模块，并使用等价类、边界值、场景法等方法设计了 100 多条功能测试用例。

接口测试部分主要使用 Python、Requests 和 Pytest，自行搭建了一个接口自动化测试框架，实现了参数化、Fixture、Token 接口关联、日志以及数据库校验。针对 Mall 后台的登录、商品增删改查、订单查询等接口编写了 20 多条自动化用例。

UI 自动化部分使用 Selenium，并采用 Page Object Model 对页面对象进行了封装，完成管理员登录、商品搜索、新增商品、订单发货等核心业务流程的自动化测试。

数据库验证方面，使用 PyMySQL 对新增商品、订单发货等业务场景进行接口与数据库数据一致性校验，验证了 pms_product、oms_order 等核心表的数据正确性。

性能测试使用 JMeter，对登录、商品列表查询以及混合业务场景进行了并发测试，并分析 TPS、P95 响应时间以及错误率。

最后使用 Allure 生成测试报告，并把 Pytest 集成到了 GitHub Actions，实现代码提交后的自动化回归测试。

------

# 四十一、简历项目描述最终版

## TestFlow —— Mall 电商系统自动化测试项目

**技术栈：**

Python / Pytest / Requests / Selenium / MySQL / JMeter / Allure / GitHub Actions

项目描述：

- 独立完成 Mall 电商系统后台管理的测试需求分析与测试方案设计，采用等价类、边界值、判定表及场景法设计 100+ 功能测试用例，覆盖登录、商品管理、订单管理、会员管理、权限管理等核心业务。
- 基于 Python + Requests + Pytest 搭建接口自动化测试框架，实现 Fixture、参数化、数据驱动、Token 接口关联及统一日志管理，覆盖商品增删改查、订单查询等 20+ 接口。
- 使用 MySQL + PyMySQL 对新增商品、订单发货等业务进行接口与数据库数据一致性验证，涉及 pms_product、oms_order 等核心表。
- 基于 Selenium + Page Object Model 实现 Web UI 自动化测试，覆盖登录、商品管理、订单管理等核心用户业务链路，并实现显式等待和失败自动截图。
- 使用 JMeter 对登录、商品列表查询和混合业务场景进行性能测试，通过 TPS、P95 响应时间和错误率分析系统性能表现。
- 集成 Allure 自动生成测试报告，并通过 GitHub Actions 实现代码提交后的自动化回归测试。

------

# 四十二、这个项目真正重要的地方

你不要陷入一个误区：

> 我要把 Selenium、JMeter、Docker、Jenkins、Linux、Python 全部学完以后才能开始。

不要这样。

正确方式是：

```text
遇到什么
↓
学什么
↓
马上用到项目
```

比如：

今天做接口测试。

那今天只学习：

```text
HTTP
Requests
assert
pytest
```

然后马上写代码。

明天需要数据库验证，再学：

```text
SQL
PyMySQL
```

这种方式学习效率最高。

------

# 四十三、你今天就可以开始做的事情

第一天只做四件事：

```text
1. 把 Mall 后台管理系统成功运行起来（JDK 17 + MySQL + Redis + mall-admin + mall-admin-web）

2. 建立 GitHub TestFlow-mall 仓库

3. 整理 Mall 后台全部功能模块

4. 写第一版《测试计划》
```

不要第一天就碰 Selenium 和 JMeter。

先把测试流程跑起来。

等这一步完成以后，再开始自动化。

整个项目最核心的一条路线就是：

**功能测试 → 接口测试 → Pytest框架 → 数据库验证 → Selenium → JMeter → Allure → CI → 简历。**

只要沿着这条线做完，你就会拥有一个比较完整的校招软件测试作品集。
